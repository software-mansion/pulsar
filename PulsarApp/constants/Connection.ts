export const API_SERVER_URL = 'https://pulsar-server.swmansion.com';
// export const API_SERVER_URL = 'http://localhost:8080';

export const SOCKET_SERVER_URL = 'wss://pulsar-server.swmansion.com';
// export const SOCKET_SERVER_URL = 'ws://localhost:8080';

// Standalone live-preview web app (figma/preview). Loaded by the in-app Figma
// WebView screen — the `?host=app` flag tells the preview to delegate haptics
// playback to the host instead of using the on-page audio fallback.
export const FIGMA_PREVIEW_URL = 'https://docs.swmansion.com/figma-preview/';

