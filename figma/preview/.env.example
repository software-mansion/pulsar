# Copy this file to `.env.local` (gitignored) and fill in the real value.
# Vite exposes only variables prefixed with VITE_ to the client bundle.

# Base URL of the Pulsar backend that stores live-preview payloads by token.
VITE_API_SERVER_URL=https://pulsar-server.swmansion.com
