# Pulsar KMP

This module contains the Kotlin Multiplatform version of Pulsar.

Current targets:
- Android
- iOS (`iosX64`, `iosArm64`, `iosSimulatorArm64`)

Useful commands:
- `./gradlew projects`
- `./gradlew :library:check`
- `./gradlew :library:publishToMavenCentral`
